# Refuses requests whose Host header is not in FRONTEND_ALLOWED_HOSTS.
# Included per content block rather than at the server level, so that /health
# stays reachable over loopback for the container healthcheck.
if ($frontend_host_allowed = 0) {
    return 403;
}
